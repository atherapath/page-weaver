# Test Image Chain

If you’re seeing this text, your markdown is loading correctly.

* Each image with a `_2`, `_3`, `_4`, `_5`, or `_6` suffix will be detected.  
* Slideshow runs at **6 seconds per image**.  
* Hero caption updates automatically.

Try placing your images in the same directory as this HTML file, e.g.: